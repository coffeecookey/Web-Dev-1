document.getElementById('book-listing-form').addEventListener('submit', function(event) {
    event.preventDefault();
    // Add logic to list the book (validation, sending to backend)
    alert('Book listed successfully!');
});
